import { createSlice } from '@reduxjs/toolkit';

const initialState = {};
export const dustSlice = createSlice({
  name: 'dust',
  initialState,
  reducers: {},
});

export const {} = dustSlice.actions;
